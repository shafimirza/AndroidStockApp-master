<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/action_add" android:title="@string/action_example"
        android:icon="@mipmap/ic_action_new"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionLayout="@layout/search_layout"/>
    <item android:id="@+id/action_refresh" android:title="@string/action_refresh"
        android:icon="@mipmap/ic_action_refresh"
        android:orderInCategory="100" app:showAsAction="ifRoom" />
</menu>
